type Movie {
  title: String!
  rating: Float!
  id: Int!
  summary: String!
  language: String!
  medium_cover_image: String!
}

type Query {
  currentMovies(limit: Int, rating: Float): [Movie]!
}
